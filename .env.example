# Server Configuration
HOST=127.0.0.1
PORT=8000

# Storage Paths (Relative to workspace root)
DATABASE_PATH=backend/data/knowledge.db
PROCESSED_DIR=processed
UPLOADS_DIR=data/uploads

# Model Configuration
EXTRACTOR_MODEL=gemini-3.5-flash-lite

# LLM API Keys (Optional - rules-first engine uses deterministic pattern extraction if empty)
GEMINI_API_KEY= ADD YOUR KEY HERE...................

